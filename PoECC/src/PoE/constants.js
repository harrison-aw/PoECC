// Package-wide Constants

// Used for indices into an array of PoE attributes
const MIG = 0;
const CON = 1;
const DEX = 2;
const PER = 3;
const INT = 4;
const RES = 5;

export {MIG, CON, DEX, PER, INT, RES};